name: Windows (Zip & Installer)

on:
    push:
        branches:
            - stable
    workflow_dispatch:

jobs:
    build:
        runs-on: windows-latest

        steps:
            - uses: actions/checkout@v2

            - uses: actions/setup-node@v2
              with:
                  node-version: 14.x
                  cache: npm

            - uses: subosito/flutter-action@v1.5.3
              with:
                  channel: master

            - name: 🚧 Do prerequisites
              run: |
                  flutter config --enable-windows-desktop
                  flutter pub get
                  npm ci
                  npm run hive:generate

            - name: 🔨 Building binaries
              run: npm run windows:build

            - name: 📦 Packing as zip
              run: npm run windows:build:zip

            - name: 📦 Packing as installer
              run: npm run windows:build:installer

            - name: 🚀 Upload zip
              uses: actions/upload-artifact@v2
              with:
                  name: windows-zip
                  path: build/packed/windows/*.zip
                  if-no-files-found: error

            - name: 🚀 Upload installer
              uses: actions/upload-artifact@v2
              with:
                  name: windows-installer
                  path: build/packed/windows/*.exe
                  if-no-files-found: error
