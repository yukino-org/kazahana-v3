abstract class Defaults {
  static const Duration mouseOverlayDuration = Duration(seconds: 5);

  static const Duration animationsSlower = Duration(milliseconds: 300);
  static const Duration animationsSlow = Duration(milliseconds: 250);
  static const Duration animationsNormal = Duration(milliseconds: 200);
  static const Duration animationsFast = Duration(milliseconds: 150);
  static const Duration animationsFaster = Duration(milliseconds: 100);

  static const Duration cachedAnimeInfoExpireTime = Duration(days: 1);
  static const Duration cachedMangaInfoExpireTime = Duration(days: 1);
}
