String replaceFirstString(
  final String data,
  final String from,
  final String to,
) =>
    data.replaceFirst(from, to);

String replaceAllString(
  final String data,
  final String from,
  final String to,
) =>
    data.replaceAll(from, to);
